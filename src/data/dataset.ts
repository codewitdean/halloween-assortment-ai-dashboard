/** Expectations belong to this supplied dataset, not the reusable parser. */
export const suppliedDataset = {
  filename: 'Halloween Assortment — Team Calculator & Scenario Comparison (1).xlsx',
  expectedProductCount: 26,
} as const;
